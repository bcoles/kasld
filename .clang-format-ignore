*.sh
# Generated per-build offset tables, kept in #included .inc files so each
# component .c formats normally while the multi-thousand-row data — a single
# array initializer whose parse can exhaust clang-format's memory — is excluded.
src/components/offsets/*.inc
