a.out
*.o
*.gch
build
dev

# Editor / IDE / tool local config
.vscode/
.idea/
.claude/
*.swp
*.swo
*~
.DS_Store

# Fuzz workspace — seeds are regenerated by tests/fuzz/seed-from-fixtures.sh
# from the committed fixture tree, and libFuzzer grows the corpus during a
# run with machine-specific, nondeterministic discoveries. Neither belongs
# in source.
tests/fuzz/corpus/

# Test-corpus
tests/fixtures/**/verbose.txt
tests/fixtures/**/PRIVACY

## Bundles produced by extra/collect outside the corpus
kasld-bundle-*/
kasld-bundle-*.tar.gz
